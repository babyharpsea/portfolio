# Cloudflare Workers - static assets deployment
# Build first with `npm run build` (outputs to ./dist), then `npx wrangler deploy`.

name = "portfolio"
compatibility_date = "2024-09-25"

[assets]
directory = "./dist"
# Serve index.html for unknown paths (single-page app behaviour).
not_found_handling = "single-page-application"
